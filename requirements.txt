# Единственная сторонняя зависимость проекта — нужна только для окна-меню
# (Alt+0). Всё остальное (переключение окон, хоткеи, извлечение иконок)
# работает на стандартной библиотеке + ctypes, без сторонних пакетов.
#
# customtkinter тянет за собой Pillow — обе безопасные, широко используемые
# библиотеки с открытым кодом (MIT / HPND).
customtkinter>=5.2
