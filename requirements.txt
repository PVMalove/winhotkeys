# Сторонние зависимости нужны только для GUI (боковая панель, окно
# настроек). Всё остальное (переключение окон, хоткеи, извлечение иконок)
# работает на стандартной библиотеке + ctypes, без сторонних пакетов.
customtkinter>=5.2

# Pillow — извлечение реальных иконок .exe (winhotkeys/icons.py) для
# панели и окна настроек. Не всегда тянется как транзитивная зависимость
# customtkinter (зависит от её версии) — указана явно.
Pillow

# PySide6 (боковая панель) — нативные Qt-виджеты. Изначально панель
# рендерилась через pywebview/WebView2 (HTML/CSS), но прозрачные/
# скруглённые окна там ненадёжны на части систем (см.
# docs/adr/0004-panel-renders-via-qt.md) — заменено на Qt, где это
# работает "из коробки".
PySide6>=6.5
